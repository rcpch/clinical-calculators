version: "3.8"

services:
  api:
    # Dev-only env file. This file is intentionally separate so CI doesn't
    # require a .env in the repository root. Developers run `docker compose`
    # locally and will explicitly include this file which points to `./.env`.
    env_file:
      - ./.env
